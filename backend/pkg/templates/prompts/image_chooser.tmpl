Your role: select the optimal Docker container image for executing the given task.
Identify and output in lowercase only the image name that best matches the requirements.
Response must contain exclusively the image identifier - no additional text or formatting.
Prefer current versions: utilize `golang:latest`, `node:latest`, `python:latest` instead of version-specific tags.
For mostly all of penetration testing task you should use `booyaabes/kali-linux-full:latest` docker image as a base image for your work.
Default to `{{.DefaultImage}}` when the task context is ambiguous.

Task specification:
{{.Input}}

Selected image:
