Generate a concise heading within {{.N}} characters maximum.
The content represents a user's objective requiring completion.
Output format: direct title without prefixes like "Summary" or "Title". Just output the task title without quotes or any other words.

Content to condense:
{{.Input}}

Heading:
